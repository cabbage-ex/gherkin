defmodule Gherkin.KeywordsTest do
  use ExUnit.Case

  # alias Gherkin.Keywords

  # test "can detect right keyword" do
  #   assert {:ok, "text"} == Keywords.extract_feature_keyword("Feature text")
  #   assert {:ok, "text"} == Keywords.extract_feature_keyword("Business Need text")
  #   assert {:ok, "text"} == Keywords.extract_feature_keyword("Ability text")
  #   assert {:error, :no_match} == Keywords.extract_feature_keyword("Example text")

  #   assert {:ok, "text"} == Keywords.extract_scenario_keyword("Scenario text")
  #   assert {:ok, "text"} == Keywords.extract_scenario_keyword("Example text")
  #   assert {:error, :no_match} == Keywords.extract_scenario_keyword("Ability text")

  #   assert {:ok, "text"} == Keywords.extract_examples_keyword("Scenarios text")
  #   assert {:ok, "text"} == Keywords.extract_examples_keyword("Examples text")
  #   assert {:error, :no_match} == Keywords.extract_examples_keyword("Example text")

  #   assert {:ok, "text"} == Keywords.extract_scenario_outline_keyword("Scenario Outline text")
  #   assert {:ok, "text"} == Keywords.extract_scenario_outline_keyword("Scenario Template text")
  #   assert {:error, :no_match} == Keywords.extract_scenario_outline_keyword("Example text")

  #   assert {:ok, "text"} == Keywords.extract_rule_keyword("Rule text")
  #   assert {:error, :no_match} == Keywords.extract_rule_keyword("Example text")

  #   assert {:ok, "text"} == Keywords.extract_background_keyword("Background text")
  #   assert {:error, :no_match} == Keywords.extract_background_keyword("Example text")

  #   assert {:ok, "text"} == Keywords.extract_given_keyword("Given text")
  #   assert {:ok, "text"} == Keywords.extract_given_keyword("* text")
  #   assert {:error, :no_match} == Keywords.extract_given_keyword("Example text")

  #   assert {:ok, "text"} == Keywords.extract_when_keyword("When text")
  #   assert {:ok, "text"} == Keywords.extract_when_keyword("* text")
  #   assert {:error, :no_match} == Keywords.extract_when_keyword("Example text")

  #   assert {:ok, "text"} == Keywords.extract_then_keyword("Then text")
  #   assert {:ok, "text"} == Keywords.extract_then_keyword("* text")
  #   assert {:error, :no_match} == Keywords.extract_then_keyword("Example text")

  #   assert {:ok, "text"} == Keywords.extract_and_keyword("And text")
  #   assert {:ok, "text"} == Keywords.extract_and_keyword("* text")
  #   assert {:error, :no_match} == Keywords.extract_and_keyword("Example text")

  #   assert {:ok, "text"} == Keywords.extract_but_keyword("But text")
  #   assert {:ok, "text"} == Keywords.extract_but_keyword("* text")
  #   assert {:error, :no_match} == Keywords.extract_but_keyword("Example text")
  # end
end
